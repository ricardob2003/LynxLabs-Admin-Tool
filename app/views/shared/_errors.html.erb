<% if object.errors.any? %>
  <section class="errors">
    <h2>
      Oops! La Información no se pudo guardar.
    </h2>
    <h3>
      Porfavor corriga los siguientes errores:
      <%= pluralize(object.errors.size, "error") %>:
    </h3>
    <ul>
      <% object.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </section>
<% end %>
